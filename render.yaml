services:
  - name: moodtune-backend
    type: web
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true